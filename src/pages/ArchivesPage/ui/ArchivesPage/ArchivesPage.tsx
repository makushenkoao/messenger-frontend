import { classNames } from '@/shared/lib/classNames/classNames';
import cls from './ArchivesPage.module.scss';

const ArchivesPage = () => {
    return (
        <div className={classNames(cls.ArchivesPage, {})}>ARCHIVES PAGE</div>
    );
};
export default ArchivesPage;
